{
  "name": "@silverhand/tools-shell",
  "version": "0.0.0",
  "description": "Shared shell/runtime contracts for website tools.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "precommit": "lint-staged",
    "dev": "tsc -w -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No tests yet for @silverhand/tools-shell\""
  }
}
